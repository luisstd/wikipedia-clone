{
  "devDependencies": {
    "autoprefixer": "^10.2.6",
    "postcss": "^8.3.5",
    "tailwindcss": "^2.2.2"
  },
  "dependencies": {
    "postcss-cli": "^8.3.1"
  },
  "scripts": {
    "dev": "npx tailwindcss -o ./src/style.css --watch --jit --purge='./*.html'",
    "build": "NODE_ENV=production npx tailwindcss -o ./dist/style.css --minify --purge='*.html'"
  }
}
